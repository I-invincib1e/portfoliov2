import React from 'react';
import styled from 'styled-components';
import { useTheme } from '../context/ThemeContext';

// Remove the theme toggle component entirely
const ThemeSwitch: React.FC = () => {
  return null;
};

export default ThemeSwitch;